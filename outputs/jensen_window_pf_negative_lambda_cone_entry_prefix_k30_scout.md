# Jensen-Window PF Negative-Lambda Cone-Entry Prefix Scout

Date: 2026-07-06

Status: finite negative-lambda prefix certificate. This is not a proof of zeta
cone entry, monotone contractions, Jensen-window PF-infinity, RH, or the
Newman-direction goal.

Artifact kind: `jensen_window_pf_negative_lambda_cone_entry_prefix_scout`.

Proof boundary: this artifact certifies only a finite large-negative-lambda
prefix. It does not prove an infinite cone-entry theorem, does not prove a
collared finite flow theorem, does not control all `k`, does not prove
`jwpf_06`, and does not settle `Lambda <= 0`.

Machine-readable certificate:

```text
work/rh_compute/results/jensen_window_pf_negative_lambda_cone_entry_prefix_k30_scout.json
```

Coefficient enclosures:

```text
work/rh_compute/results/acb_enclosures_negative_lambda_cone_entry_lam_m25_m50_m100_k0_k30.jsonl
work/rh_compute/results/acb_enclosures_negative_lambda_cone_entry_lam_m25_m50_m100_k0_k30_summary.json
```

Generator:

```text
python work/rh_compute/scripts/jensen_window_pf_negative_lambda_cone_entry_prefix_scout.py
```

Checker:

```text
python work/rh_compute/scripts/check_jensen_window_pf_negative_lambda_cone_entry_prefix_scout.py --target work/rh_compute/results/jensen_window_pf_negative_lambda_cone_entry_prefix_k30_scout.json --note outputs/jensen_window_pf_negative_lambda_cone_entry_prefix_k30_scout.md
```

Current result:

```text
validated Jensen-window PF negative-lambda cone-entry prefix scout: 93 coefficient rows, 87 lower-wall rows, 87 upper-wall rows, 84 monotone-gap rows, 0 issues
```

## Scope

The checked ratios are:

```text
x_k=(A_{k+1}/A_k)/(A_k/A_{k-1})
(2*k-1)/(2*k+1) <= x_k <= 1
x_{k+1} >= x_k
```

The finite prefix is:

```text
lambdas: -100.0, -50.0, -25.0
coefficient range: A_0..A_30
lower and upper walls: 1 <= k <= 29
monotone wall: 1 <= k <= 28
```

The coefficient enclosures were generated by:

```text
python work/rh_compute/scripts/acb_coefficient_enclosures.py --lambdas=-25,-50,-100 --k-min 0 --k-max 30 --n-sum 50 --cutoff 6 --dps 160 --digits 80 --abs-tol 1e-110 --constant-terms 30 --output-dir work/rh_compute/results --run-id acb_enclosures_negative_lambda_cone_entry_lam_m25_m50_m100_k0_k30 --overwrite
```

## Certified Margins

```text
lower wall rows: 87 / 87
upper wall rows: 87 / 87
monotone gap rows: 84 / 84

minimum lower-wall margin:
  2.281558922837392072E-2 at lambda=-25.0, k=29

minimum upper-wall margin:
  5.492799667254455944E-3 at lambda=-100.0, k=29

minimum monotone gap:
  5.743772179246203517E-5 at lambda=-100.0, k=28
```

## Consequence

This moves the cone-entry route from purely formal fixed-k asymptotics to a
finite interval-certified large-negative-lambda prefix. The remaining theorem
work is unchanged in kind: prove an all-`k` tail/uniform statement, or prove a
finite-prefix collar theorem strong enough to feed the conditional heat-flow
ratio-cone invariance lemma.

Related artifacts:

```text
outputs/jensen_window_pf_heat_flow_cone_entry_asymptotic_target.md
outputs/jensen_window_pf_phi_taylor_cone_entry_sign_scout.md
outputs/jensen_window_pf_heat_flow_ratio_cone_invariance_lemma.md
```
