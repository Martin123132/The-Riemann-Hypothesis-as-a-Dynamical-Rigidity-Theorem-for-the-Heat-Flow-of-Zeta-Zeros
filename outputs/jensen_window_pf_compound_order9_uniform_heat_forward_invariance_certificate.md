# Order-nine uniform-heat forward-invariance certificate

Date: 2026-07-14

Status: signed contiguous and arbitrary-column order-nine theorem on `-100<=lambda<=0`; This is not a proof of the all-degree Jensen bridge, PF-infinity, RH,
or `Lambda <= 0`; it proves one fixed compound layer.

## Cooperative propagation

Endpoint entry: `Q_(9,n)(-100)=H_(9,n)(-100)>0 for every integer n>=0`.

Uniform tail: `there exists N_9 such that Q_(9,n)(lambda)>0 for every n>=N_9 and -100<=lambda<=0`.

Cooperative flow: `Q_n'=a_n*Q_(n+1)+b_n*Q_n, a_n=(4*n+66)*Q_(8,n)/Q_(8,n+1)>0, b_n=((4*n+66)/(4*n+62))*(log Q_(8,n+1))'`.

Variation of constants and finite confinement then prove

`Q_(9,n)(lambda)=H_(9,n)(lambda)>0 for every n>=0 and -100<=lambda<=0`.

## Arbitrary columns

The completed signed contiguous layers through order nine satisfy the
fixed-order initial-minor hypotheses pointwise in `lambda`, giving

`epsilon_9*R_(9,n)(j_1,j_2,j_3,j_4,j_5,j_6,j_7,j_8,j_9;lambda)>0 for every n>=0, 0<=j_1<j_2<j_3<j_4<j_5<j_6<j_7<j_8<j_9, and -100<=lambda<=0`.

## Boundary

`derive a uniform-in-order endpoint and forward mechanism; fixed order nine alone is not PF-infinity`.
